{
  "name": "rw-cloudflare-workers",
  "version": "1.0.0",
  "description": "Repository for Cloudflare Workers containers designed for Claude Code web",
  "main": "index.js",
  "scripts": {
    "create-worker": "node scripts/create-worker.js",
    "test": "echo \"No tests yet\" && exit 0",
    "deploy": "wrangler deploy",
    "deploy:hello-world": "cd workers/hello-world && npm run deploy",
    "deploy:api-proxy": "cd workers/api-proxy && npm run deploy",
    "deploy:claude-agent": "cd workers/claude-agent && npm run deploy",
    "dev": "wrangler dev"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "edge",
    "serverless"
  ],
  "author": "myselfgus",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0"
  }
}
